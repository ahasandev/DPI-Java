<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DPI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <!-- <item name="android:windowAnimationStyle">@style/ActivityAnim</item>-->
    </style>

    <style name="Theme.DPI" parent="Base.Theme.DPI" />
    <!--  <style name="ActivityAnim" parent="@android:style/Animation.Activity">
       <item name="android:activityOpenEnterAnimation">@android:anim/bounce_interpolator</item>
       <item name="android:activityOpenExitAnimation">@android:anim/bounce_interpolator</item>

      <item name="android:activityCloseEnterAnimation">@android:anim/bounce_interpolator</item>
      <item name="android:activityCloseExitAnimation">@android:anim/bounce_interpolator</item>
   </style>-->
</resources>